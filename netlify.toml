[build]
  # Ensure devDependencies (craco) are installed before building
  command = "cd frontend && yarn install --production=false && yarn build"
  publish = "frontend/build"

[context.production.environment]
  # Set your API URL in Netlify dashboard instead of here for security
  # REACT_APP_API_URL = "https://abil-backend-xxxx.render.com"
